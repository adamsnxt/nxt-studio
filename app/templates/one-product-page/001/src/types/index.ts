interface Hero {
  title: string;
  heroImg: string;
  cta: {
    paragraph: string;
    button: string;
  };
  productImg: [string, string, string];
}

interface WhatIs {
  title: string;
  paragraph: string;
}
